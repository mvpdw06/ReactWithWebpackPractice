import React from 'react';
import ReactDOM from 'react-dom';

import MainComponent from './components/MainComponent';

const rootEl = document.getElementById('app');

ReactDOM.render(
    <MainComponent  />, rootEl
);